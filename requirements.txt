# Python 3.12 虚拟环境使用（PaddleOCR 暂不支持 3.13+）
paddlepaddle>=2.6
paddleocr>=2.7
mss>=9.0
numpy
Pillow
requests
keyring>=24.0        # API 密钥存 Windows 凭据管理器（分发场景各自填写）
pywebview>=5.0       # Web UI 容器（WebView2）
