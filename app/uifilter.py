"""OCR 结果兜底过滤：识别文本若全由本工具自身 UI 文案组成，说明截图混入了
工具面板（涂黑排除失效的极端场景），直接丢弃，防止把工具界面翻译进游戏。"""
import re

# 本工具自身 UI 的文案词表（按钮/状态/标题等）
_UI_WORDS = (
    "f5", "f6", "f7", "f8", "f9", "f10", "f11",
    "截图直译", "立即翻译", "游戏窗口", "框选区域", "选区", "窗口",
    "暂停", "恢复", "后端", "显示", "覆盖原文", "独立面板",
    "监控中", "已暂停", "等待选择区域", "捕获游戏窗口",
    "正在识别", "正在翻译", "翻译完成", "截图发送中",
    "视觉模型", "翻译中", "思考过程", "译文",
    "按阅读顺序", "整屏发给", "含思考过程",
    "llm", "deepl", "ollama",
    "f6立即翻译", "f7窗口", "f8选区", "f9暂停", "f10后端", "f11显示",
    "游戏实时翻译", "输出历史", "提问", "带画面", "问", "未监控", "显示切换", "游戏",
    "输出面板", "桌面歌词", "歌词", "自动翻译", "隐藏译文", "实时翻译", "对话",
)


def is_own_ui_text(text):
    """规范化后能被 UI 词表完全拆解 → 是自身文案（≤60 字符的短块才可能是）"""
    norm = re.sub(r"[\s()\[\]{}:：|·，,。.\-●○✓⟳⏸]+", "", text).lower()
    if not norm or len(norm) > 60:
        return False
    changed = True
    while changed and norm:
        changed = False
        for w in _UI_WORDS:
            if w in norm:
                norm = norm.replace(w, "", 1)
                changed = True
    return norm == ""
